# Legacy prototype

`index.html` — single-file **PâtisCoût** calculator uploaded before **GramWise** bootstrap.

Kept for reference only. Pricing logic is **not** the v1 engine; see `docs/TEST_CASES.md` and upcoming `engine/`.
